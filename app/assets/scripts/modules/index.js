const changeTitle = require('./setTitle');
const changeColorOrWidth = require('./setColorOrWidth');
const changeAxisTitle = require('./setAxisTitle');
const randomlyRemoveDatapoints = require('./randomlyRemoveDatapoints');

module.exports = exports = {
  changeTitle,
  changeColorOrWidth,
  changeAxisTitle,
  randomlyRemoveDatapoints
};
